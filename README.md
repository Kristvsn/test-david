# 🧪 Todo Test Framework

Фреймворк для тренировки автоматизированного тестирования API `/todos`, написанный на Python с использованием `pytest`, `requests` и кастомных утилит.

## 🚀 Быстрый старт

Установи зависимости:
``` bash
pip install -r requirements.txt
```

Запусти тесты:
``` bash
pytest -v
```
Запусти с отчетом:
``` bash
pytest -m ui --html=report.html
```

## 🧰 Используемые технологии

- Python 3.x
- pytest
- requests
- logging
- кастомные обёртки: BaseApi, TodoApi
- утилиты: Assertions, Helpers

## 📁 Фикстуры

Фикстуры `get_todo_data` и `get_default_count_todo_items` должны быть определены в `conftest.py`.

## 📌 Заметки

- Все ассерт-функции вынесены в `helpers/assertions.py`
- Для выборки случайных задач используется `helpers/helpers.py`
- Код покрыт аннотациями типов и снабжён docstring’ами
- Подходит для расширения: можно добавить методы POST, PUT, DELETE, валидацию схем, интеграцию с Allure
